# Reading Guide 2.2

- Replaced the popup trigger with a real anchor link (`#readingSystemModal`).
- Added a CSS `:target` fallback, so the guide opens even if another JavaScript module fails.
- Added high-priority visibility, pointer-event, and z-index rules.
- Added hash-change, backdrop, close-link, and Escape handling.
- Added a visible **v2.2** badge to distinguish this build from cached/older copies.
